package org.example.hit.heal.oriantation.feature.presentation

import androidx.compose.runtime.Composable
import androidx.compose.runtime.remember
import cafe.adriel.voyager.navigator.Navigator
import org.example.hit.heal.oriantation.data.model.OrientationTestViewModel
//
//@Composable
//fun OrientationTestNavigation() {
//    // Create a single instance of the ViewModel that will be shared across all screens
//    val viewModel = remember { OrientationTestViewModel() }
//
//    // Start with the WelcomeScreen and pass the ViewModel to it
//    Navigator(WelcomeScreen(viewModel)) { navigator ->
//        // This is where you can handle navigation events if needed
//        navigator.push(WelcomeScreen(viewModel))
//    }
//}